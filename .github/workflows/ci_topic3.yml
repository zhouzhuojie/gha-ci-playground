name: ci/topic3

on:
  pull_request:
  push:
    branches:
      - master
      - main

jobs:
  label_guard:
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || contains(github.event.pull_request.labels.*.name, 'ci/topic3')
    steps:
      - run: echo "noop"
  topic3_gha_job1:
    needs: [label_guard]
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "topic3_gha_job1"